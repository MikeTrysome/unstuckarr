services:
  unstuckarr:
    image: dockersftw/unstuckarr:latest   # use pre-built image from Docker Hub
    # build: .                            # uncomment to build locally instead
    ports:
      - "7676:7676"
    volumes:
      - ./data:/data
    env_file: .env
    restart: unless-stopped
